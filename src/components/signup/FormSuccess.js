import React from "react";

const FormSuccess = ({ text }) => (
  <section className="form-success">
    <p className="text-success">
      <img src="" alt="icon form" />
      <span className="text">{text}</span>
    </p>
  </section>
);

export default FormSuccess;
